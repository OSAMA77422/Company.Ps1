namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            1- when check something and want to return bad reques {return BadRequest("Message")}
            2- to get value of nalluble variable of any type ==> variable.value
            3- to return not found data ==> return NotFound("Message" or new{anonymus type})
            4- to send paramter to action from another action use ag helper ==> asp-route-info = "@variable_name"
            5- use readonly in razor page to make things doesnt allow editing
            6- use annotaion with post method called ==> [validateantiforgerytoken] to avoid any one send request out of my web application like by postman or any tool or software
            7- htmlform support only get and post method in controller
             */
        }
    }
}
